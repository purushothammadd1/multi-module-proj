server:
  port: 9091

spring:
  application:
    name: payment-service
  datasource:
    url: jdbc:mysql://paymentdb:3306/payments?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimeZone=UTC
    username: root
    password: payments
    hikari:
      initialization-fail-timeout: 0
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

stripe:
  key: ${STRIPE_SECRET_KEY}